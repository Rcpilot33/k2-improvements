# Detect the actual Creality Print prime-tower toolpath so adaptive mesh and
# KAMP can reserve its footprint before the print starts.
[prime_tower]
# Extra clearance around the path centerline, in millimeters.
padding: 0.5
