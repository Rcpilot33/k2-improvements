KAMP replaces the fixed front-left purge line with a purge placed near
the current print and inside the usable bed area.

Before using KAMP:
  1. Enable "Label objects" in your slicer.
  2. Use the bundled machine-start G-code template for your slicer.
  3. Confirm the template contains M109 before LINE_PURGE.

Creality Print templates:
  slicer-templates/creality-start-material-only.gcode
  slicer-templates/creality-start-material-kamp.gcode
  slicer-templates/creality-start-material-surface-profiles.gcode
  slicer-templates/creality-start-material-surface-profiles-kamp.gcode

Orca template:
  slicer-templates/orca-machine-start.gcode

The installer does not restart Klipper. When no print is active, run
FIRMWARE_RESTART and wait for the complete K2 startup sequence. Power-cycle
before the next G28 only if that restart reports an error.
